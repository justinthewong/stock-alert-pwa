# IB Gateway credentials (copy to .env and fill in)
TWS_USERID=your_ibkr_username
TWS_PASSWORD=your_ibkr_password
TRADING_MODE=paper
READ_ONLY_API=yes
TIME_ZONE=Australia/Sydney
AUTO_RESTART_TIME=11:59 PM
TWOFA_TIMEOUT_ACTION=restart
RELOGIN_AFTER_TWOFA_TIMEOUT=yes

# Enables the in-dashboard IB Gateway GUI popup (VNC). Required for 2FA / challenge-response in the browser.
VNC_SERVER_PASSWORD=choose-a-vnc-password

# App -> IB Gateway connection (4004 paper, 4003 live inside docker network)
IB_GATEWAY_PORT=4004
IB_GATEWAY_CONTAINER=stock-alert-ib-gateway
IB_CLIENT_ID=1
IB_MAX_DEPTH_SYMBOLS=3
IB_VNC_HOST=ib-gateway
IB_VNC_PORT=5900
